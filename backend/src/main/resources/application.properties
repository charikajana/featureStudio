spring.application.name=bdd-editor-backend

# Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/bdd_editor}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Security Configuration (Basic setup, to be refined)
spring.security.user.name=admin
spring.security.user.password=admin

# File Storage Configuration
app.workspace.root=${APP_WORKSPACE_ROOT:workspaces}

# Logging Configuration (enable debug for PipelineService)
logging.level.com.editor.backend.service.PipelineService=DEBUG

# External Integration Configuration
app.azure.devops.base-url=https://dev.azure.com
app.azure.devops.api-version=7.1-preview.1
app.github.api-url=https://api.github.com
app.github.base-url=https://github.com
app.github.identifiers=github.com
app.azure.devops.identifiers=dev.azure.com,visualstudio.com
